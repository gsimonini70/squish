# Stamped at package time by Maven resource filtering (see pom.xml <resources>).
# Do NOT edit these values by hand and do NOT read them directly — go through BuildInfo,
# which handles the unfiltered-placeholder / missing-file fallbacks.
build.version=@project.version@
build.commit=@git.commit@
build.time=@build.time@
